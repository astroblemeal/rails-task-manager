

<h1> <%= @task.title %></h1>
<h1> <%= @task.details %></h1>

<%= link_to "Back to all tasks", tasks_path, class:"btn btn-primary" %>
<%= link_to "Delete task", task_path(@task), method: :delete, data: { confirm: "Why are you like this?" }, class:"btn btn-primary" %>
<%= link_to "Edit Task", edit_task_path(@task), class:"btn btn-primary" %>